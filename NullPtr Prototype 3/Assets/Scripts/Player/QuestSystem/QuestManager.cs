using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class QuestManager : MonoBehaviour
{
    // Update is called once per frame
    void Update()
    {
        foreach (QuestHolder.CurrentQuest)
        {

        }
    }
}
